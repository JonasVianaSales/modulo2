function dividir() {
    var qt_alunos = parseInt(document.getElementById('quant_alunos');
    var texto = '';
    for (i = 0);
}

function contar() {
    var listaAlunos = document.querySelectorAll(".notaProva");
    console.log(listaAlunos);
    console.log("Segue a lista:" + listaAlunos[0].value);
    console.log("Segue a lista:" + listaAlunos[1].value);
    
}